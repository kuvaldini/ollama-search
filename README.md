# ./ollama-search

the radically simplest shell sript replaces missed `ollama search` subcommand for ollama cmd line tool.

This project is an extension for Ollama, a platform for running large language models (LLMs) locally.

## LISENCE
MIT or WTFPL
